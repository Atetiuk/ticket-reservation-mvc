<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title %> | Rezerwacje Wydarzeń</title>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap');
    * {
      box-sizing: border-box;
    }
    body {
      margin: 0;
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: #f0f0f5;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 16px;
    }
    header {
      max-width: 900px;
      width: 100%;
      padding: 16px 24px;
      background: rgba(255 255 255 / 0.1);
      border-radius: 12px;
      backdrop-filter: blur(10px);
      box-shadow: 0 8px 24px rgba(0,0,0,0.2);
      margin-bottom: 32px;
      display: flex;
      align-items: center;
      gap: 12px;
    }
    header h1 {
      font-weight: 600;
      font-size: 1.5rem;
      color: #ffffffdd;
      margin: 0;
    }
    main {
      max-width: 900px;
      width: 100%;
      flex: 1;
      background: rgba(255 255 255 / 0.1);
      padding: 24px 32px;
      border-radius: 16px;
      backdrop-filter: blur(12px);
      box-shadow: 0 8px 40px rgba(0,0,0,0.3);
    }
    footer {
      max-width: 900px;
      width: 100%;
      margin-top: 48px;
      padding: 16px 32px;
      text-align: center;
      font-size: 0.9rem;
      color: #ccccccbb;
      user-select: none;
    }
    a {
      color: #c4b5fd;
      text-decoration: none;
      transition: color 0.3s ease;
    }
    a:hover,
    a:focus {
      color: #a78bfa;
      outline: none;
    }
    button, select, input[type="number"] {
      font-family: inherit;
      font-size: 1rem;
      border-radius: 8px;
      border: none;
      padding: 10px 14px;
      margin-top: 6px;
      background: rgba(255 255 255 / 0.15);
      color: #eee;
      box-shadow: inset 0 0 6px rgba(0,0,0,0.3);
      transition: background-color 0.25s ease;
      min-width: 100px;
      cursor: pointer;
    }
    button:hover, select:hover, input[type="number"]:hover,
    button:focus, select:focus, input[type="number"]:focus {
      background: rgba(255 255 255 / 0.3);
      outline: none;
    }
    label {
      display: block;
      margin-top: 16px;
      font-weight: 600;
      color: #ddd;
    }
    .material-icons {
      vertical-align: middle;
    }
  </style>
</head>
<body>
  <header>
    <span class="material-icons" aria-hidden="true" style="font-size: 28px; color: #a78bfa;">event</span>
    <h1>System Rezerwacji Biletów na Wydarzenia</h1>
  </header>
  <main role="main"><%- body %></main>
  <footer>© 2024 System Rezerwacji Biletów. Wszystkie prawa zastrzeżone.</footer>
</body>
</html>